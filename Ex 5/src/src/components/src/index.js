import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import "./index.css"; // optional if you use Tailwind or your own CSS

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
